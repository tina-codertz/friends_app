 //we define all colors to be used in the app globally in here to avoid code duplication and to make it easier to change the colors later
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0A0A0F);
  static const Color cardBackground = Color(0xFF1A1A2E);
  static const Color cyan = Color(0xFF00F5FF);
  static const Color pink = Color(0xFFE0B0FF);
  static const Color purple = Color(0xFF7B2FBE);
  static const Color purpleDark = Color(0xFF2D1B4E);
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF888888);
  static const Color inputBackground = Color(0xFF2A2A3E);
}